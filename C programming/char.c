#include<stdio.h>
#include<ctype.h>
int main(){
    char character;
    printf("Enter a character");
     scanf("%c", &character);
     if ("character "){
    printf("The characrer %c is a digit\n",character);
} else {
        printf("The character %c is not a digit\n",character);
}
    
    return 0;
    }